express = require 'express'
router = express.Router()

module.exports = router = express.Router()

# router.get '/', (req, res) ->
#   res.render 'index', 
#     title: 'express use the nice router'

# router.get '/markdown', (req, res) ->
#   res.render 'index.md'

# router.get '/blogs/:title.html', (req, res) ->
#   path = ['blogs/', req.params.title, '.md'].join('')
#   console.log path
#   res.render path
